// Arithmetic Operator mutation

// The implementation of the function is incorrect
// But the test suite passes
// mutation testing will help us catch this
export function multiply(a: number, b: number): number {
    return a / b;
}