// Unary Operator mutation

// The implementation of the function is incorrect
// But the test suite passes

export function calculateAverage(a: number, b: number): number {
    return (a + b) * 2;
}
